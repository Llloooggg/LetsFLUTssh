# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
.metadata
devtools_options.yaml

# Local tool cache (actionlint, etc — see Makefile)
.cache/

# Claude Code session-local files
.claude/scheduled_tasks.lock

# IDE
.idea/
*.iml
.vscode/
*.swp
*.swo
*~

# macOS
.DS_Store
macos/Flutter/ephemeral/
macos/Pods/

# iOS
ios/Flutter/ephemeral/
ios/Pods/
ios/.symlinks/

# Android
android/.gradle/
android/local.properties
android/app/debug/
android/app/profile/
android/app/release/
**/android/key.properties
*.jks

# Linux
linux/flutter/ephemeral/

# Web (not targeted)
web/

# Windows
windows/flutter/ephemeral/

# Generated
*.g.dart
*.freezed.dart
# Exception: FRB ships sealed-class bindings as committed Freezed
# files alongside the Dart bindings. Without this exception, every
# fresh checkout would have to install the FRB codegen CLI + run
# `make rust-codegen` before the app could even compile.
!lib/src/rust/**/*.freezed.dart

# Fuzz (compiled native targets)
fuzz/out/

# Coverage
/coverage/
*.lcov

# Secrets
*.env
.env.*

# pubspec.lock — committed for apps (not packages)
# pubspec.lock

# Logs
*.log

# Claude (local-only files)
.claude/settings.local.json
.claude/memory/
.claude/plans/
.claude/worktrees/

# Local-only review / audit artefacts — per maintainer these never ship
# with the repo (they go stale fast, bloat history). Keep them as
# working docs, commit nothing.
docs/REVIEW_*.md

# Local planning doc — kept untracked intentionally.
SECURITY_BACKLOG.md

# Rust core — build artefacts (security/transport core, see rust/)
rust/target/
