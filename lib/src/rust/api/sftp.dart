// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'ssh.dart';

// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `clone`, `clone`, `fmt`, `fmt`, `from`, `from`

/// Open an SFTP subsystem on a fresh channel of the given session.
/// Multiple SFTP clients can coexist on one SSH session — each call
/// allocates a new channel.
Future<SshSftp> sshOpenSftp({required SshSession session}) =>
    RustLib.instance.api.crateApiSftpSshOpenSftp(session: session);

/// Open a remote file for reading. Use `SshSftpFile::read_chunk`
/// to pump bytes, or `metadata` first to grab `size` for progress
/// reporting.
Future<SshSftpFile> sshSftpOpen({
  required SshSftp sftp,
  required String path,
}) => RustLib.instance.api.crateApiSftpSshSftpOpen(sftp: sftp, path: path);

/// Open a remote file for writing, truncating any existing content.
Future<SshSftpFile> sshSftpCreate({
  required SshSftp sftp,
  required String path,
}) => RustLib.instance.api.crateApiSftpSshSftpCreate(sftp: sftp, path: path);

// Rust type: RustOpaqueMoi<flutter_rust_bridge::for_generated::RustAutoOpaqueInner<SshSftp>>
abstract class SshSftp implements RustOpaqueInterface {
  /// Resolve a path against the server's working directory.
  /// Expands `~` / relative paths the remote shell would resolve.
  Future<String> canonicalize({required String path});

  /// List a directory.
  Future<List<SftpDirEntry>> list({required String path});

  /// Create a directory (single level — caller walks for `mkdir -p`).
  Future<void> mkdir({required String path});

  /// Read a small file fully into memory. Use the streaming surface
  /// (sub-phase 1.5b) for files larger than a few MB.
  Future<Uint8List> readFile({required String path});

  /// Remove an empty directory.
  Future<void> removeDir({required String path});

  /// Remove a regular file.
  Future<void> removeFile({required String path});

  /// Rename / move.
  Future<void> rename({required String oldPath, required String newPath});

  /// Stat a path (resolves symlinks).
  Future<SftpFileMetadata> stat({required String path});

  /// Stat a path without resolving symlinks.
  Future<SftpFileMetadata> statSymlink({required String path});

  /// Overwrite a small file with `data`.
  Future<void> writeFile({required String path, required List<int> data});
}

// Rust type: RustOpaqueMoi<flutter_rust_bridge::for_generated::RustAutoOpaqueInner<SshSftpFile>>
abstract class SshSftpFile implements RustOpaqueInterface {
  /// Stat the open handle (no extra round-trip).
  Future<SftpFileMetadata> metadata();

  /// Read up to `max_bytes` starting at the current cursor. Empty
  /// `Vec` signals EOF.
  Future<Uint8List> readChunk({required int maxBytes});

  /// Move the cursor to `offset` bytes from the start of the file.
  Future<void> seek({required BigInt offset});

  /// Flush + fsync (best-effort — server may ignore).
  Future<void> syncAll();

  /// Write the entire `data` slice at the current cursor.
  Future<void> writeAll({required List<int> data});
}

/// One directory entry surfaced by `SshSftp::list`.
class SftpDirEntry {
  final String name;
  final BigInt size;
  final bool isDir;
  final bool isSymlink;

  /// Unix epoch seconds, `null` on the Dart side when the server
  /// omitted mtime or a translation failed.
  final PlatformInt64? modifiedUnix;

  /// POSIX mode bits (e.g. 0o755). `0` when unavailable.
  final int permissions;

  const SftpDirEntry({
    required this.name,
    required this.size,
    required this.isDir,
    required this.isSymlink,
    this.modifiedUnix,
    required this.permissions,
  });

  @override
  int get hashCode =>
      name.hashCode ^
      size.hashCode ^
      isDir.hashCode ^
      isSymlink.hashCode ^
      modifiedUnix.hashCode ^
      permissions.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SftpDirEntry &&
          runtimeType == other.runtimeType &&
          name == other.name &&
          size == other.size &&
          isDir == other.isDir &&
          isSymlink == other.isSymlink &&
          modifiedUnix == other.modifiedUnix &&
          permissions == other.permissions;
}

/// File metadata surfaced by `SshSftp::stat` / `stat_symlink`.
class SftpFileMetadata {
  final BigInt size;
  final bool isDir;
  final bool isSymlink;
  final PlatformInt64? modifiedUnix;
  final int permissions;

  const SftpFileMetadata({
    required this.size,
    required this.isDir,
    required this.isSymlink,
    this.modifiedUnix,
    required this.permissions,
  });

  @override
  int get hashCode =>
      size.hashCode ^
      isDir.hashCode ^
      isSymlink.hashCode ^
      modifiedUnix.hashCode ^
      permissions.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SftpFileMetadata &&
          runtimeType == other.runtimeType &&
          size == other.size &&
          isDir == other.isDir &&
          isSymlink == other.isSymlink &&
          modifiedUnix == other.modifiedUnix &&
          permissions == other.permissions;
}
