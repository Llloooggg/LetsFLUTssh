// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:freezed_annotation/freezed_annotation.dart' hide protected;
part 'enclave.freezed.dart';

// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_fields_are_eq`, `assert_fields_are_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`

/// Probe whether SE-SSH is reachable on this host. Runs a real
/// generate / delete round trip so the wizard can route the user
/// at the code-signing remediation when the bundle isn't trusted.
Future<DbEnclaveAvailability> enclaveSshProbe() =>
    RustLib.instance.api.crateApiEnclaveEnclaveSshProbe();

/// Generate a fresh SE-bound ECDSA P-256 key + persist it as an
/// `ssh_keys` row. Fires the system biometric / passcode prompt at
/// the OS layer per the chosen [`DbEnclaveAuthPolicy`]. Returns
/// the new row id + the `authorized_keys` line the wizard
/// surfaces alongside the "copy" affordance.
Future<DbEnclaveImportResult> enclaveSshGenerate({
  required DbEnclaveGenerateArgs args,
}) => RustLib.instance.api.crateApiEnclaveEnclaveSshGenerate(args: args);

/// Enumerate SE-bound keys the chip holds today. Used by the
/// "orphan recovery" path — `ssh_keys` was wiped but the chip
/// kept its register; the dialog renders one row per orphan with
/// a "delete" affordance.
Future<List<DbEnclaveOrphan>> enclaveSshListOrphans() =>
    RustLib.instance.api.crateApiEnclaveEnclaveSshListOrphans();

/// Delete the on-chip SE-bound key referenced by `ssh_keys.id`.
/// Also soft-deletes the DB row so the manager view drops it.
/// Best-effort on the chip side — `SecItemDelete` failures don't
/// roll back the DB tombstone; the OS GCs orphaned keys on next
/// launch.
Future<void> enclaveSshDelete({required String keyId}) =>
    RustLib.instance.api.crateApiEnclaveEnclaveSshDelete(keyId: keyId);

/// FRB mirror of `apple_se_ssh::AuthPolicy`. The wizard's radio
/// step writes one of these into the create call.
enum DbEnclaveAuthPolicy {
  /// Require Touch ID / Face ID; re-enrolment invalidates the
  /// key.
  biometryCurrentSet,

  /// Allow device passcode as fallback; survives re-enrolment.
  userPresence,
}

@freezed
sealed class DbEnclaveAvailability with _$DbEnclaveAvailability {
  const DbEnclaveAvailability._();

  /// Reachable — wizard renders enabled.
  const factory DbEnclaveAvailability.available() =
      DbEnclaveAvailability_Available;

  /// Bundle is ad-hoc / unsigned; `errSecMissingEntitlement`
  /// (-34018). Dart shows the localized "Code-signing required"
  /// reason + links the user to USER_GUIDE.md.
  const factory DbEnclaveAvailability.codeSignRequired() =
      DbEnclaveAvailability_CodeSignRequired;

  /// Pre-T2 Intel Mac or simulator — no SE hardware.
  const factory DbEnclaveAvailability.noSecureEnclave() =
      DbEnclaveAvailability_NoSecureEnclave;

  /// Device passcode unset.
  const factory DbEnclaveAvailability.passcodeNotSet() =
      DbEnclaveAvailability_PasscodeNotSet;

  /// Any other failure — carries the diagnostic string.
  const factory DbEnclaveAvailability.other(String field0) =
      DbEnclaveAvailability_Other;

  /// Build target without Apple support (Linux, Windows,
  /// Android). The toolbar action stays hidden.
  const factory DbEnclaveAvailability.unsupportedPlatform() =
      DbEnclaveAvailability_UnsupportedPlatform;
}

/// Bundled input for [`enclave_ssh_generate`].
class DbEnclaveGenerateArgs {
  final String label;
  final DbEnclaveAuthPolicy policy;

  const DbEnclaveGenerateArgs({required this.label, required this.policy});

  @override
  int get hashCode => label.hashCode ^ policy.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbEnclaveGenerateArgs &&
          runtimeType == other.runtimeType &&
          label == other.label &&
          policy == other.policy;
}

/// FRB mirror of `apple_se_ssh::EnclaveKeyHandle` rendered into the
/// import wizard's preview card. Carries the assigned DB row id
/// alongside the on-chip metadata so the caller can refresh the
/// key-manager listing without a second FRB hop.
class DbEnclaveImportResult {
  final String keyId;
  final String label;

  /// `authorized_keys`-shaped line — `ecdsa-sha2-nistp256 BASE64
  /// <label>` — the wizard copies into the clipboard for the
  /// user to paste on the server. The same line is persisted as
  /// `ssh_keys.public_key`.
  final String authorizedKeysLine;

  const DbEnclaveImportResult({
    required this.keyId,
    required this.label,
    required this.authorizedKeysLine,
  });

  @override
  int get hashCode =>
      keyId.hashCode ^ label.hashCode ^ authorizedKeysLine.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbEnclaveImportResult &&
          runtimeType == other.runtimeType &&
          keyId == other.keyId &&
          label == other.label &&
          authorizedKeysLine == other.authorizedKeysLine;
}

/// FRB mirror of an SE-bound key seen by the OS but absent from
/// our DB. Surfaced by [`enclave_ssh_list`]; the recovery dialog
/// renders one row per orphan with a "delete" affordance so the
/// chip stays clean after a DB wipe.
class DbEnclaveOrphan {
  final Uint8List applicationTag;

  const DbEnclaveOrphan({required this.applicationTag});

  @override
  int get hashCode => applicationTag.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbEnclaveOrphan &&
          runtimeType == other.runtimeType &&
          applicationTag == other.applicationTag;
}
