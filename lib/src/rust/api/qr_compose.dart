// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'archive.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`

/// Compose the v4 payload + deflate + base64url and return the
/// byte count. Same wire shape + alphabet as
/// `db_export_qr_payload` (production export); both producers
/// route through `lfs_core::qr_compose::compose_qr_payload`.
///
/// Used by the Dart `unified_export_controller` for the live
/// "fits in QR" gauge — single sync FRB call replaces the
/// per-toggle Dart-side JSON build + Rust deflate round-trip.
int qrEstimateExportSize({required DbQrPayloadInput input}) =>
    RustLib.instance.api.crateApiQrComposeQrEstimateExportSize(input: input);

/// FRB mirror of `qr_compose::QrFolderTagLink`.
class DbQrFolderTagLink {
  final String folderPath;
  final String tagId;

  const DbQrFolderTagLink({required this.folderPath, required this.tagId});

  @override
  int get hashCode => folderPath.hashCode ^ tagId.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbQrFolderTagLink &&
          runtimeType == other.runtimeType &&
          folderPath == other.folderPath &&
          tagId == other.tagId;
}

/// FRB mirror of `qr_compose::QrManagerKeyEntry`.
class DbQrManagerKeyEntry {
  final String id;
  final String label;
  final String keyType;
  final String publicKey;
  final String privateKey;

  const DbQrManagerKeyEntry({
    required this.id,
    required this.label,
    required this.keyType,
    required this.publicKey,
    required this.privateKey,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      label.hashCode ^
      keyType.hashCode ^
      publicKey.hashCode ^
      privateKey.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbQrManagerKeyEntry &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          label == other.label &&
          keyType == other.keyType &&
          publicKey == other.publicKey &&
          privateKey == other.privateKey;
}

/// FRB mirror of `qr_compose::QrPayloadInput`. Crosses the
/// boundary as a flat struct so the Dart caller can build it
/// inline from the export-dialog selections.
class DbQrPayloadInput {
  final DbQrExportOptions options;
  final List<DbQrSessionInput> sessions;
  final List<String> emptyFolders;
  final String? configJson;
  final String knownHosts;
  final List<DbQrTagInput> tags;
  final List<DbQrSessionTagLink> sessionTags;
  final List<DbQrFolderTagLink> folderTags;
  final List<DbQrSnippetInput> snippets;
  final List<DbQrSessionSnippetLink> sessionSnippets;
  final List<DbQrManagerKeyEntry> managerKeyEntries;

  const DbQrPayloadInput({
    required this.options,
    required this.sessions,
    required this.emptyFolders,
    this.configJson,
    required this.knownHosts,
    required this.tags,
    required this.sessionTags,
    required this.folderTags,
    required this.snippets,
    required this.sessionSnippets,
    required this.managerKeyEntries,
  });

  @override
  int get hashCode =>
      options.hashCode ^
      sessions.hashCode ^
      emptyFolders.hashCode ^
      configJson.hashCode ^
      knownHosts.hashCode ^
      tags.hashCode ^
      sessionTags.hashCode ^
      folderTags.hashCode ^
      snippets.hashCode ^
      sessionSnippets.hashCode ^
      managerKeyEntries.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbQrPayloadInput &&
          runtimeType == other.runtimeType &&
          options == other.options &&
          sessions == other.sessions &&
          emptyFolders == other.emptyFolders &&
          configJson == other.configJson &&
          knownHosts == other.knownHosts &&
          tags == other.tags &&
          sessionTags == other.sessionTags &&
          folderTags == other.folderTags &&
          snippets == other.snippets &&
          sessionSnippets == other.sessionSnippets &&
          managerKeyEntries == other.managerKeyEntries;
}

/// FRB mirror of `qr_compose::QrSessionInput`. Folder paths,
/// passwords, key bytes, key-id refs are all pre-resolved by the
/// Dart caller (matches the in-memory composition the controller
/// already does for the dummy-session estimator path).
class DbQrSessionInput {
  final String id;
  final String label;
  final String host;
  final PlatformInt64 port;
  final String user;
  final String authType;
  final String password;
  final String? keyId;
  final String keyData;
  final String folderPath;

  const DbQrSessionInput({
    required this.id,
    required this.label,
    required this.host,
    required this.port,
    required this.user,
    required this.authType,
    required this.password,
    this.keyId,
    required this.keyData,
    required this.folderPath,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      label.hashCode ^
      host.hashCode ^
      port.hashCode ^
      user.hashCode ^
      authType.hashCode ^
      password.hashCode ^
      keyId.hashCode ^
      keyData.hashCode ^
      folderPath.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbQrSessionInput &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          label == other.label &&
          host == other.host &&
          port == other.port &&
          user == other.user &&
          authType == other.authType &&
          password == other.password &&
          keyId == other.keyId &&
          keyData == other.keyData &&
          folderPath == other.folderPath;
}

/// FRB mirror of `qr_compose::QrSessionSnippetLink`.
class DbQrSessionSnippetLink {
  final String sessionId;
  final String snippetId;

  const DbQrSessionSnippetLink({
    required this.sessionId,
    required this.snippetId,
  });

  @override
  int get hashCode => sessionId.hashCode ^ snippetId.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbQrSessionSnippetLink &&
          runtimeType == other.runtimeType &&
          sessionId == other.sessionId &&
          snippetId == other.snippetId;
}

/// FRB mirror of `qr_compose::QrSessionTagLink`.
class DbQrSessionTagLink {
  final String sessionId;
  final String tagId;

  const DbQrSessionTagLink({required this.sessionId, required this.tagId});

  @override
  int get hashCode => sessionId.hashCode ^ tagId.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbQrSessionTagLink &&
          runtimeType == other.runtimeType &&
          sessionId == other.sessionId &&
          tagId == other.tagId;
}

/// FRB mirror of `qr_compose::QrSnippetInput`.
class DbQrSnippetInput {
  final String id;
  final String title;
  final String command;
  final String description;

  const DbQrSnippetInput({
    required this.id,
    required this.title,
    required this.command,
    required this.description,
  });

  @override
  int get hashCode =>
      id.hashCode ^ title.hashCode ^ command.hashCode ^ description.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbQrSnippetInput &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          title == other.title &&
          command == other.command &&
          description == other.description;
}

/// FRB mirror of `qr_compose::QrTagInput`.
class DbQrTagInput {
  final String id;
  final String name;
  final String? color;

  const DbQrTagInput({required this.id, required this.name, this.color});

  @override
  int get hashCode => id.hashCode ^ name.hashCode ^ color.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbQrTagInput &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          name == other.name &&
          color == other.color;
}
