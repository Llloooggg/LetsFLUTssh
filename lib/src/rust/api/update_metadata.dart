// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `clone`, `clone`, `clone`, `fmt`, `fmt`, `fmt`, `from`

DbVersionOrder updateCompareVersions({required String a, required String b}) =>
    RustLib.instance.api.crateApiUpdateMetadataUpdateCompareVersions(
      a: a,
      b: b,
    );

bool updateIsTrustedReleaseAssetUri({required String uri}) => RustLib
    .instance
    .api
    .crateApiUpdateMetadataUpdateIsTrustedReleaseAssetUri(uri: uri);

String? updateAssetSuffix({required String platform}) => RustLib.instance.api
    .crateApiUpdateMetadataUpdateAssetSuffix(platform: platform);

String? updateParseAssetVersion({required String assetName}) => RustLib
    .instance
    .api
    .crateApiUpdateMetadataUpdateParseAssetVersion(assetName: assetName);

/// Pick the release asset URL whose `name` ends with the
/// platform's expected suffix (`asset_suffix`). Returns `None` for
/// unknown platforms or when no asset matches. Standalone FRB
/// surface kept around for tests; the production update-check
/// path goes through `update_check_from_body`, which calls the
/// underlying `lfs_core::update::metadata::asset_url_for_platform`
/// directly.
String? updateAssetUrlForPlatform({
  required List<DbReleaseAsset> assets,
  required String platform,
}) => RustLib.instance.api.crateApiUpdateMetadataUpdateAssetUrlForPlatform(
  assets: assets,
  platform: platform,
);

List<DbSha256ManifestEntry> updateParseSha256Manifest({
  required String content,
}) => RustLib.instance.api.crateApiUpdateMetadataUpdateParseSha256Manifest(
  content: content,
);

String? updateBuildCumulativeChangelog({
  required List<DbChangelogRelease> releases,
  required String currentVersion,
}) => RustLib.instance.api.crateApiUpdateMetadataUpdateBuildCumulativeChangelog(
  releases: releases,
  currentVersion: currentVersion,
);

/// FRB mirror of `ChangelogRelease`. Carries the tag + raw
/// release body as the Dart caller pre-extracted them from the
/// GitHub API JSON.
class DbChangelogRelease {
  final String tag;
  final String body;

  const DbChangelogRelease({required this.tag, required this.body});

  @override
  int get hashCode => tag.hashCode ^ body.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbChangelogRelease &&
          runtimeType == other.runtimeType &&
          tag == other.tag &&
          body == other.body;
}

/// Single GitHub release asset entry — `(name,
/// browser_download_url)`. Caller flattens the GitHub Releases
/// API JSON's `assets` array into this shape before calling
/// [`update_asset_url_for_platform`].
class DbReleaseAsset {
  final String name;
  final String browserDownloadUrl;

  const DbReleaseAsset({required this.name, required this.browserDownloadUrl});

  @override
  int get hashCode => name.hashCode ^ browserDownloadUrl.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbReleaseAsset &&
          runtimeType == other.runtimeType &&
          name == other.name &&
          browserDownloadUrl == other.browserDownloadUrl;
}

/// Single `(name, hash)` pair from the manifest. Returned as a
/// list because FRB's HashMap support varies across language
/// targets — pairs round-trip cleanly everywhere.
class DbSha256ManifestEntry {
  final String name;
  final String hash;

  const DbSha256ManifestEntry({required this.name, required this.hash});

  @override
  int get hashCode => name.hashCode ^ hash.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbSha256ManifestEntry &&
          runtimeType == other.runtimeType &&
          name == other.name &&
          hash == other.hash;
}

/// FRB mirror of `Ordering` — Dart enum-friendly.
enum DbVersionOrder { less, equal, greater }
