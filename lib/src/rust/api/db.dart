// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These functions are ignored because they are not marked as `pub`: `require_db`, `run_db_mut_writing_sessions_when`, `run_db_mut_writing_sessions`, `run_db_mut`, `run_db_writing_sessions_when`, `run_db_writing_sessions`, `run_db`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`

Future<List<DbSshKey>> dbSshKeysListAll() =>
    RustLib.instance.api.crateApiDbDbSshKeysListAll();

Future<DbSshKey?> dbSshKeysGet({required String id}) =>
    RustLib.instance.api.crateApiDbDbSshKeysGet(id: id);

Future<void> dbSshKeysUpsert({required DbSshKey row}) =>
    RustLib.instance.api.crateApiDbDbSshKeysUpsert(row: row);

/// Atomic full-table replace. **Don't fan out to N delete +
/// N upsert FRB hops** — that pays 2N round-trips for the same
/// outcome and opens a half-cleared-table race when a transient
/// FRB failure lands mid-loop. This single call lands the whole
/// replacement inside one rusqlite transaction.
Future<void> dbSshKeysReplaceAll({required List<DbSshKey> rows}) =>
    RustLib.instance.api.crateApiDbDbSshKeysReplaceAll(rows: rows);

Future<int> dbSshKeysDelete({required String id}) =>
    RustLib.instance.api.crateApiDbDbSshKeysDelete(id: id);

/// Composite import — Rust composes the dedup-by-fingerprint
/// lookup (public-key first, falling back to private-key) +
/// label-uniqueness + insert in one transaction. Returns the id
/// the caller should use downstream — existing on a content
/// match, freshly inserted otherwise. One FRB call so the whole
/// sequence lands as a single sqlite transaction.
Future<String> dbSshKeysImportForMerge({required DbSshKey proposed}) =>
    RustLib.instance.api.crateApiDbDbSshKeysImportForMerge(proposed: proposed);

/// Stage the stored key's private PEM bytes into the SecretStore
/// under `key.priv.<id>`. Returns `true` when bytes landed in the
/// store, `false` when the row is missing or the column is empty.
/// Plaintext does not cross the FRB boundary — Dart only sees the
/// boolean.
Future<bool> dbSshKeysStageSecret({required String keyId}) =>
    RustLib.instance.api.crateApiDbDbSshKeysStageSecret(keyId: keyId);

Future<List<DbSshKeyMetadata>> dbSshKeysListMetadata() =>
    RustLib.instance.api.crateApiDbDbSshKeysListMetadata();

/// Fetch the certificate paired with `key_id`, or `None` when the
/// key has no cert attached. Plain read — no SecretStore staging
/// hop; that lives on the connect path
/// ([`db_ssh_key_certificate_stage_secret`]).
Future<DbSshKeyCertificate?> dbSshKeyCertificateGet({required String keyId}) =>
    RustLib.instance.api.crateApiDbDbSshKeyCertificateGet(keyId: keyId);

/// Insert or replace the certificate paired with `rec.key_id`. The
/// caller must have validated the fingerprint pairing
/// (`keys_parse_openssh_cert` + match against the key's public-half
/// fingerprint) before calling — the DAO does not re-check.
Future<void> dbSshKeyCertificateUpsert({required DbSshKeyCertificate rec}) =>
    RustLib.instance.api.crateApiDbDbSshKeyCertificateUpsert(rec: rec);

/// Remove the certificate paired with `key_id`. Returns the number
/// of rows affected — `0` is a successful no-op when no cert was
/// attached.
Future<int> dbSshKeyCertificateDelete({required String keyId}) =>
    RustLib.instance.api.crateApiDbDbSshKeyCertificateDelete(keyId: keyId);

/// Every certificate row, ordered by `key_id`. Used by archive
/// export and a future "all certs" diagnostic. Most callers want
/// [`db_ssh_key_certificate_get`] instead.
Future<List<DbSshKeyCertificate>> dbSshKeyCertificatesListAll() =>
    RustLib.instance.api.crateApiDbDbSshKeyCertificatesListAll();

/// Stage the stored cert blob into the SecretStore under
/// `key.cert.<id>`. Returns `true` when bytes landed, `false` when
/// the key has no cert attached. The cert is public material, but
/// routing it through SecretStore keeps the connect cascade
/// symmetric with `db_ssh_keys_stage_secret` and avoids round-
/// tripping the bytes through the Dart heap.
Future<bool> dbSshKeyCertificateStageSecret({required String keyId}) =>
    RustLib.instance.api.crateApiDbDbSshKeyCertificateStageSecret(keyId: keyId);

Future<List<DbFolder>> dbFoldersListAll() =>
    RustLib.instance.api.crateApiDbDbFoldersListAll();

Future<void> dbFoldersUpsert({required DbFolder row}) =>
    RustLib.instance.api.crateApiDbDbFoldersUpsert(row: row);

Future<int> dbFoldersDelete({required String id}) =>
    RustLib.instance.api.crateApiDbDbFoldersDelete(id: id);

Future<int> dbFoldersDeleteAll() =>
    RustLib.instance.api.crateApiDbDbFoldersDeleteAll();

Future<int> dbFoldersToggleCollapsed({required String id}) =>
    RustLib.instance.api.crateApiDbDbFoldersToggleCollapsed(id: id);

Future<int> dbFoldersUpdateNameParent({
  required String id,
  required String name,
  String? parentId,
}) => RustLib.instance.api.crateApiDbDbFoldersUpdateNameParent(
  id: id,
  name: name,
  parentId: parentId,
);

/// Composite folder rename / move — Rust resolves the existing
/// folder by `old_path`, computes the new leaf name + new parent
/// path, ensures the new parent exists, then updates the row in
/// one transaction. The single-FRB-call shape keeps the rename +
/// re-parent atomic — the row's `parent_id` is read freshly inside
/// the transaction, so a cross-tree move never racing a stale
/// row-cache value.
///
/// Returns 1 on success, 0 when `old_path` resolves to nothing.
/// `Err` for cycle moves (folder under its own descendant).
Future<int> dbFoldersRenamePathCascade({
  required String oldPath,
  required String newPath,
  required PlatformInt64 nowMs,
}) => RustLib.instance.api.crateApiDbDbFoldersRenamePathCascade(
  oldPath: oldPath,
  newPath: newPath,
  nowMs: nowMs,
);

Future<int> dbFoldersDeleteRecursive({required String id}) =>
    RustLib.instance.api.crateApiDbDbFoldersDeleteRecursive(id: id);

Future<List<DbSession>> dbSessionsListAll() =>
    RustLib.instance.api.crateApiDbDbSessionsListAll();

Future<DbSession?> dbSessionsGet({required String id}) =>
    RustLib.instance.api.crateApiDbDbSessionsGet(id: id);

Future<void> dbSessionsUpsert({required DbSession row}) =>
    RustLib.instance.api.crateApiDbDbSessionsUpsert(row: row);

Future<int> dbSessionsDelete({required String id}) =>
    RustLib.instance.api.crateApiDbDbSessionsDelete(id: id);

/// Read the credential columns for [`session_id`] and push every
/// non-empty value straight into the process-singleton SecretStore
/// under the canonical `sess.<slot>.<id>` ids — bytes never cross
/// back to Dart. Returns metadata describing which slots were staged
/// so the caller can dispatch to the matching connect variant. Null
/// when the row no longer exists.
Future<DbStagedSecrets?> dbSessionsStageSecrets({required String sessionId}) =>
    RustLib.instance.api.crateApiDbDbSessionsStageSecrets(sessionId: sessionId);

/// Update non-credential metadata on a session row without touching
/// the `password` / `key_data` / `passphrase` columns. Lets the
/// edit dialog save a label / host / proxy change without first
/// reading the existing secret bytes back onto the Dart heap.
Future<int> dbSessionsUpdateMetadata({required DbSessionMetadata metadata}) =>
    RustLib.instance.api.crateApiDbDbSessionsUpdateMetadata(metadata: metadata);

/// Replace one credential column (`"password"` / `"key_data"` /
/// `"passphrase"`) on a session. Crosses FRB plaintext one direction
/// only (Dart → Rust → DB); pairs with `db_sessions_stage_secrets`
/// for the read direction. Empty `value` clears the slot.
Future<int> dbSessionsSetSecret({
  required String id,
  required String slot,
  required String value,
  required PlatformInt64 updatedAtMs,
}) => RustLib.instance.api.crateApiDbDbSessionsSetSecret(
  id: id,
  slot: slot,
  value: value,
  updatedAtMs: updatedAtMs,
);

/// Copy a saved session row to a new id + label, optionally
/// re-parented under [`target_folder_id`]. Credentials flow column-
/// to-column inside SQLite and never cross the FRB boundary, so the
/// duplicate path no longer carries plaintext on the Dart heap.
Future<void> dbSessionsDuplicate({
  required String srcId,
  required String newId,
  required String newLabel,
  String? targetFolderId,
  required PlatformInt64 nowMs,
}) => RustLib.instance.api.crateApiDbDbSessionsDuplicate(
  srcId: srcId,
  newId: newId,
  newLabel: newLabel,
  targetFolderId: targetFolderId,
  nowMs: nowMs,
);

/// Atomic restore from an undo-history snapshot. One transaction
/// covers: wipe live sessions + folders, rebuild the folder tree
/// from session paths + the bare empty-folder list, re-insert every
/// session under the freshly-resolved folder id. Single-FRB-call
/// shape keeps the rebuild + re-insert atomic so a partial restore
/// can't leave the DB in an inconsistent shape.
Future<void> dbSessionsRestoreSnapshot({
  required List<DbRestoreSessionInput> sessions,
  required List<String> emptyFolderPaths,
  required PlatformInt64 nowMs,
}) => RustLib.instance.api.crateApiDbDbSessionsRestoreSnapshot(
  sessions: sessions,
  emptyFolderPaths: emptyFolderPaths,
  nowMs: nowMs,
);

/// Composite duplicate — Rust composes label-uniqueness +
/// folder-path resolution + duplicate-insert in one transaction.
/// Returns the new session id. Single FRB call so a caller that
/// only knows the source id + a target folder path keeps the
/// whole rename + reparent + insert atomic against concurrent
/// label collisions.
Future<String> dbSessionsDuplicateWithPath({
  required String srcId,
  required String targetFolderPath,
  required PlatformInt64 nowMs,
}) => RustLib.instance.api.crateApiDbDbSessionsDuplicateWithPath(
  srcId: srcId,
  targetFolderPath: targetFolderPath,
  nowMs: nowMs,
);

Future<int> dbSessionsDeleteMultiple({required List<String> ids}) =>
    RustLib.instance.api.crateApiDbDbSessionsDeleteMultiple(ids: ids);

Future<int> dbSessionsDeleteAll() =>
    RustLib.instance.api.crateApiDbDbSessionsDeleteAll();

Future<int> dbSessionsMoveToFolder({
  required String sessionId,
  String? folderId,
  required PlatformInt64 updatedAtMs,
}) => RustLib.instance.api.crateApiDbDbSessionsMoveToFolder(
  sessionId: sessionId,
  folderId: folderId,
  updatedAtMs: updatedAtMs,
);

Future<int> dbSessionsMoveMultiple({
  required List<String> ids,
  String? folderId,
  required PlatformInt64 updatedAtMs,
}) => RustLib.instance.api.crateApiDbDbSessionsMoveMultiple(
  ids: ids,
  folderId: folderId,
  updatedAtMs: updatedAtMs,
);

Future<List<DbKnownHost>> dbKnownHostsListAll() =>
    RustLib.instance.api.crateApiDbDbKnownHostsListAll();

Future<DbKnownHost?> dbKnownHostsGetByHostPort({
  required String host,
  required PlatformInt64 port,
}) => RustLib.instance.api.crateApiDbDbKnownHostsGetByHostPort(
  host: host,
  port: port,
);

Future<PlatformInt64> dbKnownHostsUpsertByHostPort({
  required String host,
  required PlatformInt64 port,
  required String keyType,
  required String keyBase64,
  required PlatformInt64 addedAtMs,
}) => RustLib.instance.api.crateApiDbDbKnownHostsUpsertByHostPort(
  host: host,
  port: port,
  keyType: keyType,
  keyBase64: keyBase64,
  addedAtMs: addedAtMs,
);

Future<int> dbKnownHostsDeleteByHostPort({
  required String host,
  required PlatformInt64 port,
}) => RustLib.instance.api.crateApiDbDbKnownHostsDeleteByHostPort(
  host: host,
  port: port,
);

Future<int> dbKnownHostsClearAll() =>
    RustLib.instance.api.crateApiDbDbKnownHostsClearAll();

/// Bulk-import `content` (LetsFLUTssh + OpenSSH known_hosts wire
/// formats — see `lfs_core::known_hosts_parser::parse_line`)
/// against the running DB. Existing host:port entries are
/// preserved; only fresh rows insert. Emits a single
/// `KnownHostsChanged` bus event when at least one row landed.
Future<DbKnownHostsImportSummary> dbKnownHostsImportFromString({
  required String content,
  required PlatformInt64 nowMs,
}) => RustLib.instance.api.crateApiDbDbKnownHostsImportFromString(
  content: content,
  nowMs: nowMs,
);

/// Read `path` from disk (UTF-8) and dispatch to
/// [`db_known_hosts_import_from_string`]. The Rust I/O keeps the
/// raw bytes out of the Dart heap on the way to the parser, so a
/// curl-piped `~/.ssh/known_hosts` import never materialises in
/// the FRB layer twice. Returns the same summary the string-shape
/// import does. Missing file = `Ok(empty summary)` — matches the
/// Dart-era `importFromFile` contract.
Future<DbKnownHostsImportSummary> dbKnownHostsImportFromPath({
  required String path,
  required PlatformInt64 nowMs,
}) => RustLib.instance.api.crateApiDbDbKnownHostsImportFromPath(
  path: path,
  nowMs: nowMs,
);

/// Render every known-hosts row to the LetsFLUTssh wire format
/// (`host:port keytype base64key` per line). Used by `.lfs`
/// archive export.
Future<String> dbKnownHostsExportToString() =>
    RustLib.instance.api.crateApiDbDbKnownHostsExportToString();

Future<DbAppConfig?> dbAppConfigsGet() =>
    RustLib.instance.api.crateApiDbDbAppConfigsGet();

Future<void> dbAppConfigsUpsert({required DbAppConfig row}) =>
    RustLib.instance.api.crateApiDbDbAppConfigsUpsert(row: row);

Future<List<DbSnippet>> dbSnippetsListAll() =>
    RustLib.instance.api.crateApiDbDbSnippetsListAll();

Future<void> dbSnippetsUpsert({required DbSnippet row}) =>
    RustLib.instance.api.crateApiDbDbSnippetsUpsert(row: row);

Future<int> dbSnippetsDelete({required String id}) =>
    RustLib.instance.api.crateApiDbDbSnippetsDelete(id: id);

Future<int> dbSnippetsDeleteAll() =>
    RustLib.instance.api.crateApiDbDbSnippetsDeleteAll();

Future<List<DbSnippet>> dbSnippetsListForSession({required String sessionId}) =>
    RustLib.instance.api.crateApiDbDbSnippetsListForSession(
      sessionId: sessionId,
    );

Future<void> dbSessionSnippetsLink({
  required String sessionId,
  required String snippetId,
}) => RustLib.instance.api.crateApiDbDbSessionSnippetsLink(
  sessionId: sessionId,
  snippetId: snippetId,
);

Future<int> dbSessionSnippetsUnlink({
  required String sessionId,
  required String snippetId,
}) => RustLib.instance.api.crateApiDbDbSessionSnippetsUnlink(
  sessionId: sessionId,
  snippetId: snippetId,
);

Future<List<String>> dbSessionSnippetsListIds({required String sessionId}) =>
    RustLib.instance.api.crateApiDbDbSessionSnippetsListIds(
      sessionId: sessionId,
    );

Future<List<DbPortForwardRule>> dbPortForwardsListForSession({
  required String sessionId,
}) => RustLib.instance.api.crateApiDbDbPortForwardsListForSession(
  sessionId: sessionId,
);

Future<void> dbPortForwardsUpsert({required DbPortForwardRule row}) =>
    RustLib.instance.api.crateApiDbDbPortForwardsUpsert(row: row);

Future<int> dbPortForwardsDelete({required String id}) =>
    RustLib.instance.api.crateApiDbDbPortForwardsDelete(id: id);

Future<List<DbSftpBookmark>> dbSftpBookmarksListForSession({
  required String sessionId,
}) => RustLib.instance.api.crateApiDbDbSftpBookmarksListForSession(
  sessionId: sessionId,
);

Future<void> dbSftpBookmarksUpsert({required DbSftpBookmark row}) =>
    RustLib.instance.api.crateApiDbDbSftpBookmarksUpsert(row: row);

Future<int> dbSftpBookmarksDelete({required String id}) =>
    RustLib.instance.api.crateApiDbDbSftpBookmarksDelete(id: id);

/// Fetch the WebDAV detail row paired with `session_id`. `None`
/// when the session is not a WebDAV kind or has not been configured
/// yet — not an error.
Future<DbWebDavSessionDetails?> dbWebdavSessionDetailsGet({
  required String sessionId,
}) => RustLib.instance.api.crateApiDbDbWebdavSessionDetailsGet(
  sessionId: sessionId,
);

/// Insert or replace the WebDAV detail row for `rec.session_id`.
/// Caller stamps the matching `sessions` row with `kind = 'webdav'`
/// — the DAO does not enforce the pairing because the sync apply
/// path may need to insert detail rows ahead of the parent inside
/// one transaction.
Future<void> dbWebdavSessionDetailsUpsert({
  required DbWebDavSessionDetails rec,
}) => RustLib.instance.api.crateApiDbDbWebdavSessionDetailsUpsert(rec: rec);

/// Remove the WebDAV detail row for `session_id`. Returns the
/// number of rows affected; `0` is the idempotent no-op for a
/// session that was never a WebDAV kind. The parent session row
/// is untouched.
Future<int> dbWebdavSessionDetailsDelete({required String sessionId}) => RustLib
    .instance
    .api
    .crateApiDbDbWebdavSessionDetailsDelete(sessionId: sessionId);

/// Every WebDAV detail row, ordered by `session_id`. Used by
/// archive export and a future "all WebDAV sessions" diagnostic.
Future<List<DbWebDavSessionDetails>> dbWebdavSessionDetailsListAll() =>
    RustLib.instance.api.crateApiDbDbWebdavSessionDetailsListAll();

/// Canonical SecretStore id for a WebDAV session's password /
/// bearer token. Mirrors `lfs_core::db::webdav_sessions::webdav_secret_id`
/// for the Dart caller — the connect path needs the same shape to
/// resolve the secret on lookup.
String dbWebdavSessionDetailsSecretId({required String sessionId}) => RustLib
    .instance
    .api
    .crateApiDbDbWebdavSessionDetailsSecretId(sessionId: sessionId);

/// Fetch the S3 detail row paired with `session_id`. `None` when
/// the session is not an S3 kind or has not been configured yet —
/// not an error.
Future<DbS3SessionDetails?> dbS3SessionDetailsGet({
  required String sessionId,
}) =>
    RustLib.instance.api.crateApiDbDbS3SessionDetailsGet(sessionId: sessionId);

/// Insert or replace the S3 detail row for `rec.session_id`.
/// Caller stamps the matching `sessions` row with `kind = 's3'`.
Future<void> dbS3SessionDetailsUpsert({required DbS3SessionDetails rec}) =>
    RustLib.instance.api.crateApiDbDbS3SessionDetailsUpsert(rec: rec);

/// Remove the S3 detail row for `session_id`. Returns the number
/// of rows affected; `0` is the idempotent no-op for a session
/// that was never an S3 kind. The parent session row is untouched.
Future<int> dbS3SessionDetailsDelete({required String sessionId}) => RustLib
    .instance
    .api
    .crateApiDbDbS3SessionDetailsDelete(sessionId: sessionId);

/// Every S3 detail row, ordered by `session_id`. Used by archive
/// export.
Future<List<DbS3SessionDetails>> dbS3SessionDetailsListAll() =>
    RustLib.instance.api.crateApiDbDbS3SessionDetailsListAll();

/// Canonical SecretStore id for an S3 session's secret access
/// key. Mirrors `lfs_core::db::s3_sessions::s3_secret_id` for the
/// Dart caller — the connect path needs the same shape to resolve
/// the secret on lookup.
String dbS3SessionDetailsSecretId({required String sessionId}) => RustLib
    .instance
    .api
    .crateApiDbDbS3SessionDetailsSecretId(sessionId: sessionId);

Future<List<DbTag>> dbTagsListAll() =>
    RustLib.instance.api.crateApiDbDbTagsListAll();

Future<void> dbTagsUpsert({required DbTag row}) =>
    RustLib.instance.api.crateApiDbDbTagsUpsert(row: row);

Future<int> dbTagsDelete({required String id}) =>
    RustLib.instance.api.crateApiDbDbTagsDelete(id: id);

Future<int> dbTagsDeleteAll() =>
    RustLib.instance.api.crateApiDbDbTagsDeleteAll();

Future<List<DbTag>> dbTagsListForSession({required String sessionId}) =>
    RustLib.instance.api.crateApiDbDbTagsListForSession(sessionId: sessionId);

Future<List<DbTag>> dbTagsListForFolder({required String folderId}) =>
    RustLib.instance.api.crateApiDbDbTagsListForFolder(folderId: folderId);

Future<void> dbSessionTagsLink({
  required String sessionId,
  required String tagId,
}) => RustLib.instance.api.crateApiDbDbSessionTagsLink(
  sessionId: sessionId,
  tagId: tagId,
);

Future<int> dbSessionTagsUnlink({
  required String sessionId,
  required String tagId,
}) => RustLib.instance.api.crateApiDbDbSessionTagsUnlink(
  sessionId: sessionId,
  tagId: tagId,
);

Future<List<String>> dbSessionTagsListIds({required String sessionId}) =>
    RustLib.instance.api.crateApiDbDbSessionTagsListIds(sessionId: sessionId);

Future<void> dbFolderTagsLink({
  required String folderId,
  required String tagId,
}) => RustLib.instance.api.crateApiDbDbFolderTagsLink(
  folderId: folderId,
  tagId: tagId,
);

Future<int> dbFolderTagsUnlink({
  required String folderId,
  required String tagId,
}) => RustLib.instance.api.crateApiDbDbFolderTagsUnlink(
  folderId: folderId,
  tagId: tagId,
);

Future<List<String>> dbFolderTagsListIds({required String folderId}) =>
    RustLib.instance.api.crateApiDbDbFolderTagsListIds(folderId: folderId);

class DbAppConfig {
  final String data;
  final PlatformInt64 updatedAtMs;
  final PlatformInt64 autoLockMinutes;

  const DbAppConfig({
    required this.data,
    required this.updatedAtMs,
    required this.autoLockMinutes,
  });

  @override
  int get hashCode =>
      data.hashCode ^ updatedAtMs.hashCode ^ autoLockMinutes.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbAppConfig &&
          runtimeType == other.runtimeType &&
          data == other.data &&
          updatedAtMs == other.updatedAtMs &&
          autoLockMinutes == other.autoLockMinutes;
}

class DbFolder {
  final String id;
  final String name;
  final String? parentId;
  final PlatformInt64 sortOrder;
  final bool collapsed;
  final PlatformInt64 createdAtMs;

  const DbFolder({
    required this.id,
    required this.name,
    this.parentId,
    required this.sortOrder,
    required this.collapsed,
    required this.createdAtMs,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      name.hashCode ^
      parentId.hashCode ^
      sortOrder.hashCode ^
      collapsed.hashCode ^
      createdAtMs.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbFolder &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          name == other.name &&
          parentId == other.parentId &&
          sortOrder == other.sortOrder &&
          collapsed == other.collapsed &&
          createdAtMs == other.createdAtMs;
}

class DbKnownHost {
  final PlatformInt64 id;
  final String host;
  final PlatformInt64 port;
  final String keyType;
  final String keyBase64;
  final PlatformInt64 addedAtMs;

  const DbKnownHost({
    required this.id,
    required this.host,
    required this.port,
    required this.keyType,
    required this.keyBase64,
    required this.addedAtMs,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      host.hashCode ^
      port.hashCode ^
      keyType.hashCode ^
      keyBase64.hashCode ^
      addedAtMs.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbKnownHost &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          host == other.host &&
          port == other.port &&
          keyType == other.keyType &&
          keyBase64 == other.keyBase64 &&
          addedAtMs == other.addedAtMs;
}

/// FRB mirror of `lfs_core::known_hosts::ImportSummary`.
class DbKnownHostsImportSummary {
  final PlatformInt64 added;
  final PlatformInt64 skippedExisting;
  final PlatformInt64 skippedHashed;

  const DbKnownHostsImportSummary({
    required this.added,
    required this.skippedExisting,
    required this.skippedHashed,
  });

  @override
  int get hashCode =>
      added.hashCode ^ skippedExisting.hashCode ^ skippedHashed.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbKnownHostsImportSummary &&
          runtimeType == other.runtimeType &&
          added == other.added &&
          skippedExisting == other.skippedExisting &&
          skippedHashed == other.skippedHashed;
}

class DbPortForwardRule {
  final String id;
  final String sessionId;
  final String kind;
  final String bindHost;
  final PlatformInt64 bindPort;
  final String remoteHost;
  final PlatformInt64 remotePort;
  final String description;
  final bool enabled;
  final PlatformInt64 sortOrder;
  final PlatformInt64 createdAtMs;

  const DbPortForwardRule({
    required this.id,
    required this.sessionId,
    required this.kind,
    required this.bindHost,
    required this.bindPort,
    required this.remoteHost,
    required this.remotePort,
    required this.description,
    required this.enabled,
    required this.sortOrder,
    required this.createdAtMs,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      sessionId.hashCode ^
      kind.hashCode ^
      bindHost.hashCode ^
      bindPort.hashCode ^
      remoteHost.hashCode ^
      remotePort.hashCode ^
      description.hashCode ^
      enabled.hashCode ^
      sortOrder.hashCode ^
      createdAtMs.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbPortForwardRule &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          sessionId == other.sessionId &&
          kind == other.kind &&
          bindHost == other.bindHost &&
          bindPort == other.bindPort &&
          remoteHost == other.remoteHost &&
          remotePort == other.remotePort &&
          description == other.description &&
          enabled == other.enabled &&
          sortOrder == other.sortOrder &&
          createdAtMs == other.createdAtMs;
}

/// FRB mirror of `lfs_core::db::sessions::RestoreSessionInput`.
/// Same field set as `DbSession` but carries `folder_path`
/// instead of `folder_id` — the snapshot caller (undo history)
/// only knows the path, and the post-restore folder tree is
/// re-minted inside the same transaction so any prior id is
/// stale anyway.
class DbRestoreSessionInput {
  final String id;
  final String label;
  final String folderPath;

  /// Transport tag — empty string round-trips as the SSH default
  /// on the DAO side. See [`DbSession::kind`].
  final String kind;
  final String host;
  final PlatformInt64 port;
  final String user;
  final String authType;
  final String password;
  final String keyPath;
  final String keyData;
  final String? keyId;
  final String passphrase;
  final PlatformInt64 sortOrder;
  final String notes;
  final PlatformInt64? lastConnectedAtMs;
  final String extras;
  final String? viaSessionId;
  final String? viaHost;
  final PlatformInt64? viaPort;
  final String? viaUser;
  final PlatformInt64 createdAtMs;
  final PlatformInt64 updatedAtMs;

  const DbRestoreSessionInput({
    required this.id,
    required this.label,
    required this.folderPath,
    required this.kind,
    required this.host,
    required this.port,
    required this.user,
    required this.authType,
    required this.password,
    required this.keyPath,
    required this.keyData,
    this.keyId,
    required this.passphrase,
    required this.sortOrder,
    required this.notes,
    this.lastConnectedAtMs,
    required this.extras,
    this.viaSessionId,
    this.viaHost,
    this.viaPort,
    this.viaUser,
    required this.createdAtMs,
    required this.updatedAtMs,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      label.hashCode ^
      folderPath.hashCode ^
      kind.hashCode ^
      host.hashCode ^
      port.hashCode ^
      user.hashCode ^
      authType.hashCode ^
      password.hashCode ^
      keyPath.hashCode ^
      keyData.hashCode ^
      keyId.hashCode ^
      passphrase.hashCode ^
      sortOrder.hashCode ^
      notes.hashCode ^
      lastConnectedAtMs.hashCode ^
      extras.hashCode ^
      viaSessionId.hashCode ^
      viaHost.hashCode ^
      viaPort.hashCode ^
      viaUser.hashCode ^
      createdAtMs.hashCode ^
      updatedAtMs.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbRestoreSessionInput &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          label == other.label &&
          folderPath == other.folderPath &&
          kind == other.kind &&
          host == other.host &&
          port == other.port &&
          user == other.user &&
          authType == other.authType &&
          password == other.password &&
          keyPath == other.keyPath &&
          keyData == other.keyData &&
          keyId == other.keyId &&
          passphrase == other.passphrase &&
          sortOrder == other.sortOrder &&
          notes == other.notes &&
          lastConnectedAtMs == other.lastConnectedAtMs &&
          extras == other.extras &&
          viaSessionId == other.viaSessionId &&
          viaHost == other.viaHost &&
          viaPort == other.viaPort &&
          viaUser == other.viaUser &&
          createdAtMs == other.createdAtMs &&
          updatedAtMs == other.updatedAtMs;
}

/// FRB mirror of
/// [`lfs_core::db::s3_sessions::S3SessionRow`]. Carries the S3
/// transport-config tuple keyed by session id.
class DbS3SessionDetails {
  final String sessionId;
  final String accessKeyId;
  final String region;
  final String endpoint;
  final bool pathStyle;
  final String defaultBucket;
  final String defaultPrefix;

  const DbS3SessionDetails({
    required this.sessionId,
    required this.accessKeyId,
    required this.region,
    required this.endpoint,
    required this.pathStyle,
    required this.defaultBucket,
    required this.defaultPrefix,
  });

  @override
  int get hashCode =>
      sessionId.hashCode ^
      accessKeyId.hashCode ^
      region.hashCode ^
      endpoint.hashCode ^
      pathStyle.hashCode ^
      defaultBucket.hashCode ^
      defaultPrefix.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbS3SessionDetails &&
          runtimeType == other.runtimeType &&
          sessionId == other.sessionId &&
          accessKeyId == other.accessKeyId &&
          region == other.region &&
          endpoint == other.endpoint &&
          pathStyle == other.pathStyle &&
          defaultBucket == other.defaultBucket &&
          defaultPrefix == other.defaultPrefix;
}

class DbSession {
  final String id;
  final String label;
  final String? folderId;

  /// Transport tag — one of `"ssh"` / `"webdav"` / `"s3"`.
  /// Empty string upserts the SSH wire value (the DAO normalises
  /// before the SQL hop). Read side never returns empty because
  /// the column is `NOT NULL DEFAULT 'ssh'`.
  final String kind;
  final String host;
  final PlatformInt64 port;
  final String user;
  final String authType;
  final String password;
  final String keyPath;
  final String keyData;
  final String? keyId;
  final String passphrase;
  final PlatformInt64 sortOrder;
  final String notes;
  final PlatformInt64? lastConnectedAtMs;
  final String extras;
  final String? viaSessionId;
  final String? viaHost;
  final PlatformInt64? viaPort;
  final String? viaUser;
  final PlatformInt64 createdAtMs;
  final PlatformInt64 updatedAtMs;

  const DbSession({
    required this.id,
    required this.label,
    this.folderId,
    required this.kind,
    required this.host,
    required this.port,
    required this.user,
    required this.authType,
    required this.password,
    required this.keyPath,
    required this.keyData,
    this.keyId,
    required this.passphrase,
    required this.sortOrder,
    required this.notes,
    this.lastConnectedAtMs,
    required this.extras,
    this.viaSessionId,
    this.viaHost,
    this.viaPort,
    this.viaUser,
    required this.createdAtMs,
    required this.updatedAtMs,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      label.hashCode ^
      folderId.hashCode ^
      kind.hashCode ^
      host.hashCode ^
      port.hashCode ^
      user.hashCode ^
      authType.hashCode ^
      password.hashCode ^
      keyPath.hashCode ^
      keyData.hashCode ^
      keyId.hashCode ^
      passphrase.hashCode ^
      sortOrder.hashCode ^
      notes.hashCode ^
      lastConnectedAtMs.hashCode ^
      extras.hashCode ^
      viaSessionId.hashCode ^
      viaHost.hashCode ^
      viaPort.hashCode ^
      viaUser.hashCode ^
      createdAtMs.hashCode ^
      updatedAtMs.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbSession &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          label == other.label &&
          folderId == other.folderId &&
          kind == other.kind &&
          host == other.host &&
          port == other.port &&
          user == other.user &&
          authType == other.authType &&
          password == other.password &&
          keyPath == other.keyPath &&
          keyData == other.keyData &&
          keyId == other.keyId &&
          passphrase == other.passphrase &&
          sortOrder == other.sortOrder &&
          notes == other.notes &&
          lastConnectedAtMs == other.lastConnectedAtMs &&
          extras == other.extras &&
          viaSessionId == other.viaSessionId &&
          viaHost == other.viaHost &&
          viaPort == other.viaPort &&
          viaUser == other.viaUser &&
          createdAtMs == other.createdAtMs &&
          updatedAtMs == other.updatedAtMs;
}

/// Mirror of [`lfs_core::db::sessions::SessionMetadata`] crossing
/// FRB. Carries every column except the credential triplet so the
/// edit dialog can save metadata without reading old secret bytes
/// back onto the Dart heap.
class DbSessionMetadata {
  final String id;
  final String label;
  final String? folderId;
  final String host;
  final PlatformInt64 port;
  final String user;
  final String authType;
  final String keyPath;
  final String? keyId;
  final PlatformInt64 sortOrder;
  final String notes;
  final String extras;
  final String? viaSessionId;
  final String? viaHost;
  final PlatformInt64? viaPort;
  final String? viaUser;
  final PlatformInt64 updatedAtMs;

  const DbSessionMetadata({
    required this.id,
    required this.label,
    this.folderId,
    required this.host,
    required this.port,
    required this.user,
    required this.authType,
    required this.keyPath,
    this.keyId,
    required this.sortOrder,
    required this.notes,
    required this.extras,
    this.viaSessionId,
    this.viaHost,
    this.viaPort,
    this.viaUser,
    required this.updatedAtMs,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      label.hashCode ^
      folderId.hashCode ^
      host.hashCode ^
      port.hashCode ^
      user.hashCode ^
      authType.hashCode ^
      keyPath.hashCode ^
      keyId.hashCode ^
      sortOrder.hashCode ^
      notes.hashCode ^
      extras.hashCode ^
      viaSessionId.hashCode ^
      viaHost.hashCode ^
      viaPort.hashCode ^
      viaUser.hashCode ^
      updatedAtMs.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbSessionMetadata &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          label == other.label &&
          folderId == other.folderId &&
          host == other.host &&
          port == other.port &&
          user == other.user &&
          authType == other.authType &&
          keyPath == other.keyPath &&
          keyId == other.keyId &&
          sortOrder == other.sortOrder &&
          notes == other.notes &&
          extras == other.extras &&
          viaSessionId == other.viaSessionId &&
          viaHost == other.viaHost &&
          viaPort == other.viaPort &&
          viaUser == other.viaUser &&
          updatedAtMs == other.updatedAtMs;
}

class DbSftpBookmark {
  final String id;
  final String sessionId;
  final String remotePath;
  final String label;
  final PlatformInt64 createdAtMs;

  const DbSftpBookmark({
    required this.id,
    required this.sessionId,
    required this.remotePath,
    required this.label,
    required this.createdAtMs,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      sessionId.hashCode ^
      remotePath.hashCode ^
      label.hashCode ^
      createdAtMs.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbSftpBookmark &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          sessionId == other.sessionId &&
          remotePath == other.remotePath &&
          label == other.label &&
          createdAtMs == other.createdAtMs;
}

class DbSnippet {
  final String id;
  final String title;
  final String command;
  final String description;
  final PlatformInt64 createdAtMs;
  final PlatformInt64 updatedAtMs;

  const DbSnippet({
    required this.id,
    required this.title,
    required this.command,
    required this.description,
    required this.createdAtMs,
    required this.updatedAtMs,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      title.hashCode ^
      command.hashCode ^
      description.hashCode ^
      createdAtMs.hashCode ^
      updatedAtMs.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbSnippet &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          title == other.title &&
          command == other.command &&
          description == other.description &&
          createdAtMs == other.createdAtMs &&
          updatedAtMs == other.updatedAtMs;
}

class DbSshKey {
  final String id;
  final String label;
  final String privateKey;
  final String publicKey;
  final String keyType;
  final bool isGenerated;
  final PlatformInt64 createdAtMs;

  /// FIDO2 credential id for `sk-*` keys; `None` for software keys.
  /// The opaque CTAP2 blob the device matches against on every
  /// assertion; persisted alongside the SSH wire-format public key
  /// so the connect path can resolve it without a separate FRB hop.
  final Uint8List? credentialId;

  /// FIDO2 `application` field — the SSH RP-id (typically `ssh:`).
  /// `None` for software keys.
  final String? applicationString;

  /// User-verification bit captured at import. Drives the PIN prompt
  /// on connect.
  final bool hasUserVerification;

  /// Per-key dispatch policy for the in-process ssh-agent endpoint.
  /// Wire values: `"always"` / `"ask"` / `"deny"`. Default `"ask"`
  /// matches the schema default — the endpoint routes every
  /// SIGN_REQUEST through a Flutter confirmation dialog until the
  /// user promotes the row. Stays a String over the FRB boundary
  /// so the Dart side doesn't need a generated enum mirror; the
  /// `DbAgentPolicy` helpers below map Rust enum <-> String.
  final String agentPolicy;

  /// Backend discriminator (schema v9). Wire values:
  /// `"software"` / `"fido2"` / `"pkcs11"` / `"tpm"` / `"enclave"` /
  /// `"hello"` / `"keystore"`. Default `"software"`; the connect /
  /// agent dispatcher reads this to route to the right Signer impl.
  final String backend;

  /// RFC 7512 `pkcs11:` URI captured at import for PKCS#11 rows.
  /// `None` for every other backend. The connect path prefers this
  /// over the resolved module path so a re-plug under a different
  /// slot still resolves the right token + object.
  final String? pkcs11Uri;

  /// Resolved on-disk path of the PKCS#11 module the import wizard
  /// loaded. Cached for fast re-binding; the loader still verifies
  /// the SHA-256 matches before reuse.
  final String? pkcs11ModulePath;

  /// PKCS#11 token serial captured at import — used to confirm the
  /// same physical token is inserted before signing.
  final String? pkcs11TokenSerial;

  /// `CKA_ID` of the private-key object on the token. Opaque to
  /// Dart; passed verbatim back to Rust on sign.
  final Uint8List? pkcs11ObjectId;

  /// `CKA_LABEL` of the private-key object — human-readable;
  /// renders alongside the key-manager row.
  final String? pkcs11ObjectLabel;

  const DbSshKey({
    required this.id,
    required this.label,
    required this.privateKey,
    required this.publicKey,
    required this.keyType,
    required this.isGenerated,
    required this.createdAtMs,
    this.credentialId,
    this.applicationString,
    required this.hasUserVerification,
    required this.agentPolicy,
    required this.backend,
    this.pkcs11Uri,
    this.pkcs11ModulePath,
    this.pkcs11TokenSerial,
    this.pkcs11ObjectId,
    this.pkcs11ObjectLabel,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      label.hashCode ^
      privateKey.hashCode ^
      publicKey.hashCode ^
      keyType.hashCode ^
      isGenerated.hashCode ^
      createdAtMs.hashCode ^
      credentialId.hashCode ^
      applicationString.hashCode ^
      hasUserVerification.hashCode ^
      agentPolicy.hashCode ^
      backend.hashCode ^
      pkcs11Uri.hashCode ^
      pkcs11ModulePath.hashCode ^
      pkcs11TokenSerial.hashCode ^
      pkcs11ObjectId.hashCode ^
      pkcs11ObjectLabel.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbSshKey &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          label == other.label &&
          privateKey == other.privateKey &&
          publicKey == other.publicKey &&
          keyType == other.keyType &&
          isGenerated == other.isGenerated &&
          createdAtMs == other.createdAtMs &&
          credentialId == other.credentialId &&
          applicationString == other.applicationString &&
          hasUserVerification == other.hasUserVerification &&
          agentPolicy == other.agentPolicy &&
          backend == other.backend &&
          pkcs11Uri == other.pkcs11Uri &&
          pkcs11ModulePath == other.pkcs11ModulePath &&
          pkcs11TokenSerial == other.pkcs11TokenSerial &&
          pkcs11ObjectId == other.pkcs11ObjectId &&
          pkcs11ObjectLabel == other.pkcs11ObjectLabel;
}

/// FRB mirror of [`lfs_core::db::ssh_key_certificates::CertRecord`].
/// One certificate per stored SSH key — the Dart key-manager UI
/// surfaces the principals / validity / critical-options summary on
/// the matching row.
class DbSshKeyCertificate {
  final String keyId;
  final Uint8List certificate;
  final PlatformInt64 validAfter;
  final PlatformInt64 validBefore;
  final String principals;
  final String criticalOptions;
  final String fingerprint;

  const DbSshKeyCertificate({
    required this.keyId,
    required this.certificate,
    required this.validAfter,
    required this.validBefore,
    required this.principals,
    required this.criticalOptions,
    required this.fingerprint,
  });

  @override
  int get hashCode =>
      keyId.hashCode ^
      certificate.hashCode ^
      validAfter.hashCode ^
      validBefore.hashCode ^
      principals.hashCode ^
      criticalOptions.hashCode ^
      fingerprint.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbSshKeyCertificate &&
          runtimeType == other.runtimeType &&
          keyId == other.keyId &&
          certificate == other.certificate &&
          validAfter == other.validAfter &&
          validBefore == other.validBefore &&
          principals == other.principals &&
          criticalOptions == other.criticalOptions &&
          fingerprint == other.fingerprint;
}

/// Listing-only view of `ssh_keys` for UIs that don't need the PEM
/// bytes — key manager listing, import dedup, export-selection
/// pickers. The SHA-256 fingerprints are computed inside Rust so
/// callers can compare against scanned keys without pulling
/// plaintext through the FRB boundary.
class DbSshKeyMetadata {
  final String id;
  final String label;
  final String publicKey;
  final String keyType;
  final bool isGenerated;
  final PlatformInt64 createdAtMs;
  final String privateFingerprint;
  final String publicFingerprint;

  /// Backend discriminator (schema v9). One of `software` / `fido2` /
  /// `pkcs11` / `tpm` / `enclave` / `hello` / `keystore`. The Dart
  /// key-manager UI picks the per-backend badge variant off this
  /// string.
  final String backend;

  /// PKCS#11 module path captured at import. `None` for non-PKCS#11
  /// rows.
  final String? pkcs11ModulePath;

  /// PKCS#11 token serial captured at import.
  final String? pkcs11TokenSerial;

  /// PKCS#11 object label (`CKA_LABEL`).
  final String? pkcs11ObjectLabel;

  const DbSshKeyMetadata({
    required this.id,
    required this.label,
    required this.publicKey,
    required this.keyType,
    required this.isGenerated,
    required this.createdAtMs,
    required this.privateFingerprint,
    required this.publicFingerprint,
    required this.backend,
    this.pkcs11ModulePath,
    this.pkcs11TokenSerial,
    this.pkcs11ObjectLabel,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      label.hashCode ^
      publicKey.hashCode ^
      keyType.hashCode ^
      isGenerated.hashCode ^
      createdAtMs.hashCode ^
      privateFingerprint.hashCode ^
      publicFingerprint.hashCode ^
      backend.hashCode ^
      pkcs11ModulePath.hashCode ^
      pkcs11TokenSerial.hashCode ^
      pkcs11ObjectLabel.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbSshKeyMetadata &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          label == other.label &&
          publicKey == other.publicKey &&
          keyType == other.keyType &&
          isGenerated == other.isGenerated &&
          createdAtMs == other.createdAtMs &&
          privateFingerprint == other.privateFingerprint &&
          publicFingerprint == other.publicFingerprint &&
          backend == other.backend &&
          pkcs11ModulePath == other.pkcs11ModulePath &&
          pkcs11TokenSerial == other.pkcs11TokenSerial &&
          pkcs11ObjectLabel == other.pkcs11ObjectLabel;
}

/// Mirror of [`lfs_core::db::sessions::StagedSecrets`] crossing FRB.
class DbStagedSecrets {
  final String authType;
  final bool hasPassword;
  final bool hasKeyData;
  final bool hasPassphrase;

  const DbStagedSecrets({
    required this.authType,
    required this.hasPassword,
    required this.hasKeyData,
    required this.hasPassphrase,
  });

  @override
  int get hashCode =>
      authType.hashCode ^
      hasPassword.hashCode ^
      hasKeyData.hashCode ^
      hasPassphrase.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbStagedSecrets &&
          runtimeType == other.runtimeType &&
          authType == other.authType &&
          hasPassword == other.hasPassword &&
          hasKeyData == other.hasKeyData &&
          hasPassphrase == other.hasPassphrase;
}

class DbTag {
  final String id;
  final String name;
  final String? color;
  final PlatformInt64 createdAtMs;

  const DbTag({
    required this.id,
    required this.name,
    this.color,
    required this.createdAtMs,
  });

  @override
  int get hashCode =>
      id.hashCode ^ name.hashCode ^ color.hashCode ^ createdAtMs.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbTag &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          name == other.name &&
          color == other.color &&
          createdAtMs == other.createdAtMs;
}

/// FRB mirror of
/// [`lfs_core::db::webdav_sessions::WebDavSessionRow`]. Carries the
/// WebDAV transport-config tuple keyed by session id.
class DbWebDavSessionDetails {
  final String sessionId;
  final String baseUrl;
  final String username;

  /// `"basic"` / `"digest"` / `"bearer"`. The connect path parses
  /// this into the typed `lfs_core::webdav::AuthMethod`.
  final String authMethod;
  final String? selfSignedFingerprint;

  const DbWebDavSessionDetails({
    required this.sessionId,
    required this.baseUrl,
    required this.username,
    required this.authMethod,
    this.selfSignedFingerprint,
  });

  @override
  int get hashCode =>
      sessionId.hashCode ^
      baseUrl.hashCode ^
      username.hashCode ^
      authMethod.hashCode ^
      selfSignedFingerprint.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbWebDavSessionDetails &&
          runtimeType == other.runtimeType &&
          sessionId == other.sessionId &&
          baseUrl == other.baseUrl &&
          username == other.username &&
          authMethod == other.authMethod &&
          selfSignedFingerprint == other.selfSignedFingerprint;
}
