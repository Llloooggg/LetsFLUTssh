// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These functions are ignored because they are not marked as `pub`: `sync_err_to_wire`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `clone`, `clone`, `clone`, `fmt`, `fmt`, `fmt`, `from`, `from`, `from`

/// Synchronous snapshot — see [`lfs_core::sync::status`] for the
/// state read. Cheap (no I/O, no DB hit), safe to call from a
/// Riverpod `build()`.
DbSyncStatus syncStatus() => RustLib.instance.api.crateApiSyncSyncStatus();

/// Push the encrypted `.lfs` archive to the configured WebDAV
/// remote. See [`lfs_core::sync::push`] for the full flow.
Future<DbSyncResult> syncPush() => RustLib.instance.api.crateApiSyncSyncPush();

/// Pull the latest `.lfs` from the remote and merge it into the
/// local DB. See [`lfs_core::sync::pull`].
Future<DbSyncResult> syncPull() => RustLib.instance.api.crateApiSyncSyncPull();

/// Snapshot the persisted [`SyncConfig`]. Sync — reads from the
/// config store actor; cheap enough to call from a Riverpod
/// `build()`. Returns the default shape (`enabled = false`) when
/// the store is not yet initialised.
DbSyncConfig syncConfigGet() =>
    RustLib.instance.api.crateApiSyncSyncConfigGet();

/// Persist a new [`SyncConfig`] through the config store actor.
/// Returns `Err` when the store has not been initialised yet
/// (cold-start window). The disk write is debounced to match
/// the rest of the actor's writes (slider drag / fast typing
/// collapses to one I/O).
Future<void> syncConfigSet({required DbSyncConfig value}) =>
    RustLib.instance.api.crateApiSyncSyncConfigSet(value: value);

/// Stage `bytes` into the [`lfs_core::secrets::SecretStore`]
/// under `id`. The Settings UI uses this to push the WebDAV
/// password / sync passphrase into the SecretStore so the
/// orchestrator can read them by id without the plaintext
/// crossing the FRB boundary a second time.
Future<void> syncSecretPut({required String id, required List<int> bytes}) =>
    RustLib.instance.api.crateApiSyncSyncSecretPut(id: id, bytes: bytes);

/// True when a secret has been staged under `id`. The Settings
/// UI uses this to render the "configured" / "not configured"
/// hint without reading the secret bytes.
bool syncSecretHas({required String id}) =>
    RustLib.instance.api.crateApiSyncSyncSecretHas(id: id);

/// Drop the secret under `id`. Idempotent — calling on a
/// missing id is a no-op. Used when the user clears the
/// "WebDAV password" / "Sync passphrase" field in Settings.
Future<void> syncSecretDrop({required String id}) =>
    RustLib.instance.api.crateApiSyncSyncSecretDrop(id: id);

/// Read the live [`lfs_core::config::SyncConfig`] off the config
/// store actor so the Settings UI can render the fields without
/// hand-decoding the canonical JSON. Wire shape mirrors
/// `SyncConfig` field-for-field plus the secret-id pointers; the
/// actual secrets live in [`lfs_core::secrets::SecretStore`] and
/// never cross the FRB boundary.
class DbSyncConfig {
  final bool enabled;
  final String webdavUrl;
  final String webdavUsername;
  final String webdavPasswordRef;
  final String webdavAuthMethod;
  final String passphraseRef;
  final String remotePath;
  final PlatformInt64 lastPushedAtMs;
  final PlatformInt64 lastPulledAtMs;
  final String lastPushedSha256;
  final String lastPushedEtag;
  final String lastPulledEtag;
  final String lastPulledSha256;

  const DbSyncConfig({
    required this.enabled,
    required this.webdavUrl,
    required this.webdavUsername,
    required this.webdavPasswordRef,
    required this.webdavAuthMethod,
    required this.passphraseRef,
    required this.remotePath,
    required this.lastPushedAtMs,
    required this.lastPulledAtMs,
    required this.lastPushedSha256,
    required this.lastPushedEtag,
    required this.lastPulledEtag,
    required this.lastPulledSha256,
  });

  static Future<DbSyncConfig> default_() =>
      RustLib.instance.api.crateApiSyncDbSyncConfigDefault();

  @override
  int get hashCode =>
      enabled.hashCode ^
      webdavUrl.hashCode ^
      webdavUsername.hashCode ^
      webdavPasswordRef.hashCode ^
      webdavAuthMethod.hashCode ^
      passphraseRef.hashCode ^
      remotePath.hashCode ^
      lastPushedAtMs.hashCode ^
      lastPulledAtMs.hashCode ^
      lastPushedSha256.hashCode ^
      lastPushedEtag.hashCode ^
      lastPulledEtag.hashCode ^
      lastPulledSha256.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbSyncConfig &&
          runtimeType == other.runtimeType &&
          enabled == other.enabled &&
          webdavUrl == other.webdavUrl &&
          webdavUsername == other.webdavUsername &&
          webdavPasswordRef == other.webdavPasswordRef &&
          webdavAuthMethod == other.webdavAuthMethod &&
          passphraseRef == other.passphraseRef &&
          remotePath == other.remotePath &&
          lastPushedAtMs == other.lastPushedAtMs &&
          lastPulledAtMs == other.lastPulledAtMs &&
          lastPushedSha256 == other.lastPushedSha256 &&
          lastPushedEtag == other.lastPushedEtag &&
          lastPulledEtag == other.lastPulledEtag &&
          lastPulledSha256 == other.lastPulledSha256;
}

/// Mirror of [`SyncResult`]. The variant tag rides in `kind` so
/// the Dart side reads `result.kind == 'pushed'` without parsing
/// the localized summary.
class DbSyncResult {
  /// One of `pushed` / `pull_applied` / `up_to_date` / `skipped`.
  final String kind;
  final BigInt bytes;
  final String sha256;
  final int sessionsMerged;
  final int keysMerged;
  final int tagsMerged;
  final int snippetsMerged;
  final int bookmarksMerged;
  final String reason;

  const DbSyncResult({
    required this.kind,
    required this.bytes,
    required this.sha256,
    required this.sessionsMerged,
    required this.keysMerged,
    required this.tagsMerged,
    required this.snippetsMerged,
    required this.bookmarksMerged,
    required this.reason,
  });

  @override
  int get hashCode =>
      kind.hashCode ^
      bytes.hashCode ^
      sha256.hashCode ^
      sessionsMerged.hashCode ^
      keysMerged.hashCode ^
      tagsMerged.hashCode ^
      snippetsMerged.hashCode ^
      bookmarksMerged.hashCode ^
      reason.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbSyncResult &&
          runtimeType == other.runtimeType &&
          kind == other.kind &&
          bytes == other.bytes &&
          sha256 == other.sha256 &&
          sessionsMerged == other.sessionsMerged &&
          keysMerged == other.keysMerged &&
          tagsMerged == other.tagsMerged &&
          snippetsMerged == other.snippetsMerged &&
          bookmarksMerged == other.bookmarksMerged &&
          reason == other.reason;
}

/// Mirror of [`lfs_core::sync::SyncStatus`].
class DbSyncStatus {
  final bool enabled;
  final PlatformInt64 lastPushedAtMs;
  final PlatformInt64 lastPulledAtMs;
  final String? lastError;

  const DbSyncStatus({
    required this.enabled,
    required this.lastPushedAtMs,
    required this.lastPulledAtMs,
    this.lastError,
  });

  static Future<DbSyncStatus> default_() =>
      RustLib.instance.api.crateApiSyncDbSyncStatusDefault();

  @override
  int get hashCode =>
      enabled.hashCode ^
      lastPushedAtMs.hashCode ^
      lastPulledAtMs.hashCode ^
      lastError.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbSyncStatus &&
          runtimeType == other.runtimeType &&
          enabled == other.enabled &&
          lastPushedAtMs == other.lastPushedAtMs &&
          lastPulledAtMs == other.lastPulledAtMs &&
          lastError == other.lastError;
}
