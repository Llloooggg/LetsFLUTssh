// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:freezed_annotation/freezed_annotation.dart' hide protected;
part 'hello.freezed.dart';

// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`

/// Probe whether Hello-SSH is reachable on this host. Runs a real
/// generate / delete round trip so the wizard can route the user at
/// the correct remediation.
Future<DbHelloProbeResult> helloSshProbe() =>
    RustLib.instance.api.crateApiHelloHelloSshProbe();

/// Generate a fresh Hello-bound SSH key + persist it as an
/// `ssh_keys` row. Fires the OS Hello prompt inside the call per
/// the UI policy set at create time.
Future<DbHelloImportResult> helloSshGenerate({
  required DbHelloGenerateArgs args,
}) => RustLib.instance.api.crateApiHelloHelloSshGenerate(args: args);

/// Enumerate Hello-bound persisted keys that the TPM holds. Used by
/// the "orphan recovery" path when the DB lost the row but the chip
/// still holds it.
Future<List<DbHelloOrphan>> helloSshListOrphans() =>
    RustLib.instance.api.crateApiHelloHelloSshListOrphans();

/// Delete the Hello-bound on-TPM key referenced by `ssh_keys.id`.
/// Also soft-deletes the DB row so the manager view drops it.
/// Mirrors the Apple Secure Enclave + PKCS#11 patterns.
Future<void> helloSshDelete({required String keyId}) =>
    RustLib.instance.api.crateApiHelloHelloSshDelete(keyId: keyId);

/// FRB mirror of `lfs_os_security::windows::ncrypt_ssh::SshKeyAlgo`.
/// Stays a string-tag enum across the boundary so the Dart side can
/// pattern-match on the discriminator.
enum DbHelloAlgo {
  /// ECDSA P-256 — preferred default; widest TPM support.
  ecdsaP256,

  /// ECDSA P-384 — TPM-firmware-dependent. Probe before exposing.
  ecdsaP384,

  /// RSA-2048 PKCS#1 v1.5 — older-server fallback.
  rsa2048,
}

/// Bundled input for [`hello_ssh_generate`].
class DbHelloGenerateArgs {
  final String label;
  final DbHelloAlgo algo;

  const DbHelloGenerateArgs({required this.label, required this.algo});

  @override
  int get hashCode => label.hashCode ^ algo.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbHelloGenerateArgs &&
          runtimeType == other.runtimeType &&
          label == other.label &&
          algo == other.algo;
}

/// Result of `hello_ssh_generate`. Carries the new DB row id + the
/// authorized_keys-shaped public-key line for the user to paste on
/// the server. Same shape as the SE / PKCS#11 wizards.
class DbHelloImportResult {
  final String keyId;
  final String label;
  final String authorizedKeysLine;

  /// TPM tier resolved at create time. Surfaced so the
  /// "Software-gated" warning persists into the Complete step.
  final DbHelloTpmTier tier;

  const DbHelloImportResult({
    required this.keyId,
    required this.label,
    required this.authorizedKeysLine,
    required this.tier,
  });

  @override
  int get hashCode =>
      keyId.hashCode ^
      label.hashCode ^
      authorizedKeysLine.hashCode ^
      tier.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbHelloImportResult &&
          runtimeType == other.runtimeType &&
          keyId == other.keyId &&
          label == other.label &&
          authorizedKeysLine == other.authorizedKeysLine &&
          tier == other.tier;
}

/// Listing entry mirror of
/// `lfs_os_security::windows::ncrypt_ssh::HelloKeyHandle`. Surfaced
/// by the "orphan recovery" path the wizard offers when the DB row
/// is gone but the TPM still holds the persistent key.
class DbHelloOrphan {
  final String credentialName;
  final DbHelloAlgo algo;

  const DbHelloOrphan({required this.credentialName, required this.algo});

  @override
  int get hashCode => credentialName.hashCode ^ algo.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbHelloOrphan &&
          runtimeType == other.runtimeType &&
          credentialName == other.credentialName &&
          algo == other.algo;
}

@freezed
sealed class DbHelloProbeResult with _$DbHelloProbeResult {
  const DbHelloProbeResult._();

  /// Probe round-trip completed. `tier` says whether the host has
  /// TPM 2.0 or fell back to the software KSP.
  const factory DbHelloProbeResult.available({required DbHelloTpmTier tier}) =
      DbHelloProbeResult_Available;

  /// `MS_PLATFORM_KEY_STORAGE_PROVIDER` could not be opened. Most
  /// commonly: no TPM, GPO blocked, or Win < 10 1607.
  const factory DbHelloProbeResult.providerUnavailable(String field0) =
      DbHelloProbeResult_ProviderUnavailable;

  /// Hello is not configured — finalize step surfaced
  /// `NTE_USER_CANCELLED`. Wizard routes the user at "Settings ->
  /// Sign-in options".
  const factory DbHelloProbeResult.helloNotConfigured() =
      DbHelloProbeResult_HelloNotConfigured;

  /// Non-Windows build target. Toolbar entry stays hidden.
  const factory DbHelloProbeResult.unsupported() =
      DbHelloProbeResult_Unsupported;

  /// Any other failure — carries the diagnostic string.
  const factory DbHelloProbeResult.other(String field0) =
      DbHelloProbeResult_Other;
}

/// FRB mirror of `lfs_os_security::windows::ncrypt_ssh::TpmTier`.
/// Drives the wizard's honest-label rendering on the
/// `SoftwareKsp` rung-6 path.
enum DbHelloTpmTier {
  /// TPM-backed key — strongest binding.
  hardware,

  /// PCP software KSP fallback — honest "Software-gated" label in
  /// the UI per the capability ladder.
  softwareKsp,
}
