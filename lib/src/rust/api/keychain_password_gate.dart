// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `clone`, `clone`, `fmt`, `fmt`

DbKeychainGateSeed keychainGateRandomSeed() =>
    RustLib.instance.api.crateApiKeychainPasswordGateKeychainGateRandomSeed();

/// `HMAC-SHA-256(pepper, salt || password_utf8)` — the comparison
/// HMAC the gate stores on disk. Wire-byte parity with the Dart
/// `_computeHmac` is enforced by the Rust unit tests; any change
/// in the salt-prefix vs. trailing ordering invalidates every
/// install on disk.
Uint8List keychainGateComputeHmac({
  required List<int> pepper,
  required List<int> salt,
  required String password,
}) => RustLib.instance.api.crateApiKeychainPasswordGateKeychainGateComputeHmac(
  pepper: pepper,
  salt: salt,
  password: password,
);

/// Encode the salt + hmac pair as the JSON envelope written to
/// `security_pass_hash.bin`. Caller writes the returned string's
/// UTF-8 bytes atomically + hardens to 0600.
String keychainGateEncodeBlob({
  required List<int> salt,
  required List<int> hmac,
}) => RustLib.instance.api.crateApiKeychainPasswordGateKeychainGateEncodeBlob(
  salt: salt,
  hmac: hmac,
);

/// Parse the on-disk JSON envelope. Returns `Err` on any malformed
/// shape (bad JSON / missing fields / non-string values / invalid
/// base64 / empty decoded bytes). The Dart-side `verify` treats any
/// decode failure as a "wrong password" outcome.
DbKeychainGateBlob keychainGateDecodeBlob({required String blob}) => RustLib
    .instance
    .api
    .crateApiKeychainPasswordGateKeychainGateDecodeBlob(blob: blob);

/// FRB mirror of `lfs_core::security::keychain_password_gate::DiskBlob`.
class DbKeychainGateBlob {
  final Uint8List salt;
  final Uint8List hmac;

  const DbKeychainGateBlob({required this.salt, required this.hmac});

  @override
  int get hashCode => salt.hashCode ^ hmac.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbKeychainGateBlob &&
          runtimeType == other.runtimeType &&
          salt == other.salt &&
          hmac == other.hmac;
}

/// Generated salt + pepper bundle the gate seeds at `setPassword`
/// time. Two `OsRng` calls pinned to the master-password verifier's
/// salt source so an audit can confirm the entropy provenance from
/// one place.
class DbKeychainGateSeed {
  final Uint8List salt;
  final Uint8List pepper;

  const DbKeychainGateSeed({required this.salt, required this.pepper});

  @override
  int get hashCode => salt.hashCode ^ pepper.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DbKeychainGateSeed &&
          runtimeType == other.runtimeType &&
          salt == other.salt &&
          pepper == other.pepper;
}
